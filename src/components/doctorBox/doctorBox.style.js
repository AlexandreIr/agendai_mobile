import { COLORS } from "../../constants/theme";

const styles = {
    container:{
        padding:10,
        flexDirection:'row',
        gap:20,
        borderWidth:2,
        borderRadius:6,
        borderColor: COLORS.white,
        marginBottom:10
    },
    textContainer:{
        marginTop:10,
        gap:5,
    },
    doctorSpec:{
        opacity:.4,
    }
};

export default styles;